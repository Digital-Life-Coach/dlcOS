# dlc — dlcOS Core Skills

Bundled skills shipped under the `dlc` plugin in the `dlcos` marketplace.

| Skill | Status (v0.1.0) |
|---|---|
| `dlc-setup` | placeholder — to be authored |
| `dlc-save-plan` | placeholder — port from `save-plan` |
| `dlc-resume-plan` | placeholder — port from `resume-plan` |
| `dlc-end` | placeholder — port from `end`, strip MacCog dashboards, add Office Hours hook |
| `dlc-weekly-review` | placeholder — port from `weekly-review`, strip Daylite/FreshBooks |
| `dlc-morning-brief` | placeholder — interactive setup wizard, not runtime ingester |

Coming in v1.1: `dlc-inbox-triage` (after Justin's `inbox-triage` source skill ships).

See repo root `README.md` for install instructions.
